# 이 파일을 .env 로 복사해서 채운다. .env 는 gitignore 되어 있다 (NF-5).

ANTHROPIC_API_KEY=sk-ant-...

# Supabase 대시보드 → Connection string → Session pooler
# anon 키가 아니라 직접 Postgres 연결 문자열을 쓴다 (트랜잭션 필요, D24)
SUPABASE_DB_URL=postgresql:...
